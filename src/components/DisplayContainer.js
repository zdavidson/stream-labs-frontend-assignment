import "./DisplayContainer.scss";
import StreamDisplay from "./StreamDisplay";

const DisplayContainer = () => {
  // Stream Display
  // Stream Display Adjustments

  return (
    <div id="display-container">
      <div>DisplayContainer</div>
      <StreamDisplay />
    </div>
  );
};

export default DisplayContainer;
